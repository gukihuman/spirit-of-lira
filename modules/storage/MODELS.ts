class Models {
  // auto-imported from corresponding folders with the same names
  entities: AnyObject = {}
  components: AnyObject = {}
  modules: string[] = [] // all modules like ["WORLD", "GLOBAL", "EVENTS", ...]
}
export const MODELS = new Models()
