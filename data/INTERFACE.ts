const inter = {
  target: false,
  targetLocked: false,
  inventory: false,
  targetHealth: 0,
  targetMaxHealth: 0,
  // 📜 need it?
  input: false,
  inputFocus: false,
}
export const INTERFACE = LIB.store(inter)
